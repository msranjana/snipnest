export const metadata = {
  name: "Sort Dictionary by Value",
  description: "Sorts a dictionary by its values.",
  keywords: ["dictionary", "sort", "value"],
  contributors: ["itsbrunodev"],
};

```python
data = {"a": 3, "b": 1, "c": 2}
sorted_data = dict(sorted(data.items(), key=lambda item: item[1])) # {'b': 1, 'c': 2, 'a': 3}
```