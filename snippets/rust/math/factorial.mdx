export const metadata = {
  name: "Factorial",
  description: "Calculates the factorial of a number.",
  keywords: ["factorial", "math"],
  contributors: ["itsbrunodev"],
};

```rust
fn factorial(n: u32) -> u32 {
    match n {
        0 | 1 => 1,
        _ => n * factorial(n - 1),
    }
}
```

```rust
factorial(5); // 120
```
