export const metadata = {
  name: "Percentage Formatter",
  description: "Formats a number as a percentage.",
  keywords: ["percentage", "format", "number"],
  contributors: ["itsbrunodev"],
};

```javascript
function formatPercentage(value, decimalPlaces = 2) {
  return `${(value * 100).toFixed(decimalPlaces)}%`;
}
```

```javascript
formatPercentage(0.1234); // "12.34%"
```
