export const metadata = {
  name: "Delete Item from localStorage",
  description: "Deletes an item from localStorage.",
  keywords: ["localstorage", "storage", "delete"],
  contributors: ["itsbrunodev"],
};

```javascript
function deleteFromLocalStorage(key) {
  localStorage.removeItem(key);
}
```

```javascript
deleteFromLocalStorage("user"); // { name: "John", age: 30 }
```
