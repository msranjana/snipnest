export const metadata = {
  name: "Check if Item Exists in localStorage",
  description: "Checks if a specific item exists in localStorage.",
  keywords: ["localstorage", "storage"],
  contributors: ["ditinagrawal"],
};

```javascript
function isItemInLocalStorage(key) {
  return localStorage.getItem(key) !== null;
}
```

```javascript
isItemInLocalStorage("user"); // Output: true or false
```
